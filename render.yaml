# render.yaml (root)
services:
  - type: web
    name: emotion-backend
    env: docker
    dockerfilePath: backend/Dockerfile
    branch: main
    region: oregon
    plan: free
    autoDeploy: true
    buildCommand: ""      # leave blank for Docker builds on Render
    startCommand: ""      # leave blank; CMD in Dockerfile used
