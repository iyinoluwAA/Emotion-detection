# Web framework + CORS
Flask==3.1.1
flask-cors==4.0.0

# ML stack (cpu)
tensorflow-cpu==2.20.0
h5py>=3.7.0

# Image IO / preprocessing
Pillow>=9.0.0
opencv-python-headless==4.9.0.80

# Utilities for testing / batch scripts
requests>=2.28.0

# Production WSGI
gunicorn>=20.1.0
