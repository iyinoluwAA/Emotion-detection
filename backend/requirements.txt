# Web framework + CORS
Flask==3.1.1
flask-cors==4.0.0

# Use CPU builds of TF and pinned numpy compatible with TF 2.20
tensorflow-cpu==2.20.0
numpy==1.26.4
h5py>=3.7.0

# Image IO / preprocessing
Pillow>=9.0.0
opencv-python-headless==4.9.0.80

# Utilities
requests>=2.28.0

# Production WSGI
gunicorn>=23.0.0
